// boolean : true or false values
var condition1 = true; // Explicit
var condition2 = false; // Implicit
// number : whole numbers and floating point values
var number1 = 1; // Explicit
var number2 = 2; // Implicit
// string : text values like "chao lobi"
var str1 = "chao"; // Explicit
var str2 = "lobi"; // Implicit
// ts array
var names = [];
names.push("chao");
names.push("Lobi");
// names.push(3); // error, since we change the array element type
// ts object
var car = {
    type: "lamborghini",
    // type = "lamborghini" -> error syntax
    model: "Aventador",
    year: 12
};
